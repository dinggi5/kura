[고친 것]

• AI 연결: 디스크 이미지에서 바로 실행 중일 때, 이 맥에 설치된 Kura 가 다른 버전이면 알려줘요. AI 는 설치본 쪽을 실행하기 때문이에요.

• AI 연결: Claude Code 등록이 실패하면 기존 등록을 되돌리는 명령도 같이 보여줘요. 안내하던 수동 명령이 되돌려 둔 등록을 다시 지울 수 있었어요.

[안쪽]

• 배포 빌드에서 업데이트 서명 키와 애플 자격증명이 빌드 스크립트·의존성에 노출될 수 있던 경로를 없앴어요. 빌드를 컴파일 단계와 서명 단계로 나눠, 자격증명은 서명 단계에만 들어가요.

—

[Fixed]

• AI connection: when Kura is running straight from the disk image and the copy installed on this Mac is a different version, it tells you now — the AI runs the installed copy.

• AI connection: if registering with Claude Code fails, the screen now also shows the command that restores your previous registration. The manual command it used to suggest could delete the registration it had just restored.

[Internal]

• Removed a path where the update signing key and Apple credentials could be exposed to build scripts and dependencies during a release build. The build is now split into a compile step and a signing step, and credentials enter only the signing step.

바뀐 코드 전부 · full diff: https://github.com/dinggi5/kura/compare/v0.2.1...v0.2.2
